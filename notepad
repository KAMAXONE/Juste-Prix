<head>
<title>="Juste prix"</title>
</head>
<body>
<div id="question"> Proposez un prix (max 200)</div>
<div id="entree"></div>
<input type='text' 
id="sortie"> Plus grand </div>
<script arc=^projet.js">
<div id='sortie'></div>







function getRandomIt (max) {
  return Math.floor(Math.random() * max);
}
question=document.getElementByld('question');
entrees=document.getElementByld('entree');
sortie=document.getElementByld('sortie');
entree.addEventListener('keydown',test);
prixmax=getRandomInt(1000000);
prixinconnu=getRandomInt(prixmax);
question.innerHTML='Proposez un prix (maximum = '+ prixmax.toString()

function test(e) {
    let reponse='';
    if (e.key=='Enter'){
	    console.loq(prixinconnu);
	    if (entree.text==prixinconnu){reponse='gagne'};
	    if (prix<prixinconnu) {reponse='plus petit'};
        if (prix>prixinconnu) {reponse='plus grand'};
		sortie.innerHTML=reponse;
	};
		


prixmax=getRandomInt(prixmax);
